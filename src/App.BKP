import React, {Component} from 'react';
import './App.css';

class App extends Component{
  render(){
    const load = false;
    const show = true;
    const name = "Sinu";
    const loading = "Loading..."
    return (
      <div className="App">
        <h1>Hello {load ? loading : show && name }</h1>
      </div>
    );
  }
}

export default App;
